{
  "id": "survival-analysis",
  "name": "Survival / Kaplan-Meier Analysis",
  "description": "Kaplan-Meier curves and Cox proportional hazards modeling",
  "type": "template",
  "version": "1.0.0",
  "tags": [
    "survival-analysis",
    "clinical-trials",
    "kaplan-meier"
  ],
  "languages": [
    "python"
  ],
  "priority": 5,
  "capabilities": {
    "inspectFiles": false,
    "providesTools": false,
    "validatesOutput": false,
    "providesTemplate": true,
    "modifiesSystemPrompt": false
  },
  "inputSchema": [],
  "outputSchema": [],
  "payload": {
    "type": "template",
    "guidance": "When performing survival analysis, ensure time and event columns\nare correctly identified. Use lifelines for Python. Always include\nconfidence intervals and risk tables.",
    "templateCode": "import pandas as pd\nfrom lifelines import KaplanMeierFitter, CoxPHFitter\nfrom lifelines.statistics import logrank_test\nimport matplotlib.pyplot as plt\n\ndf = pd.read_csv(\"clinical_data.csv\")\n# Expect columns: time, event (1=event, 0=censored), group\n\nkmf = KaplanMeierFitter()\nfig, ax = plt.subplots(figsize=(10, 6))\n\nfor grp in df['group'].unique():\n    mask = df['group'] == grp\n    kmf.fit(df.loc[mask, 'time'], df.loc[mask, 'event'], label=grp)\n    kmf.plot_survival_function(ax=ax, ci_show=True)\n\nax.set_xlabel(\"Time\")\nax.set_ylabel(\"Survival Probability\")\nax.set_title(\"Kaplan-Meier Survival Curves\")\nplt.savefig(\"km_curves.png\", dpi=100, bbox_inches=\"tight\")\nplt.close()\n\n# Log-rank test between groups\ngroups = df['group'].unique()\nif len(groups) == 2:\n    g1 = df[df['group'] == groups[0]]\n    g2 = df[df['group'] == groups[1]]\n    lr = logrank_test(g1['time'], g2['time'], g1['event'], g2['event'])\n    print(f\"Log-rank test p-value: {lr.p_value:.4e}\")"
  },
  "enabled": true,
  "visibility": "builtin",
  "createdAt": "2026-03-08T20:47:54.759Z",
  "updatedAt": "2026-03-09T01:18:57.631Z"
}
