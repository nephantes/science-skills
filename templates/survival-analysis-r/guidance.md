When performing survival analysis in R, use the survival package.
If survminer is available, use ggsurvplot for publication-quality plots.
Otherwise use base plot(). Always include confidence intervals and p-values.